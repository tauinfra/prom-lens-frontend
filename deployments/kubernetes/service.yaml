apiVersion: v1
kind: Service
metadata:
  name: prom-lens-frontend
  labels:
    app.kubernetes.io/name: prom-lens-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prom-lens-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
