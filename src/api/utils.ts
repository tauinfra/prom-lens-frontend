export const authnUrlApi = (url: string) => `/api/v1/authn/${url}`;
export const auditUrlApi = (url: string) => `/api/v1/audit/${url}`;
export const kubeUrlApi = (url: string) => `/api/v1/kubernetes/${url}`;
export const promUrlApi = (url: string) => `/api/v1/prometheus/${url}`;
export const dragonUrlApi = (url: string) => `/api/v1/dragon/${url}`;
